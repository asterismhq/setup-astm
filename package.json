{
  "name": "setup-astm",
  "version": "2.0.0",
  "description": "GitHub Action for setting up the Asterism (astm) CLI",
  "private": true,
  "packageManager": "pnpm@11.1.3",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=24.14.0 <25"
  },
  "scripts": {
    "check": "biome check . && pnpm run typecheck",
    "fix": "biome check --write .",
    "clean": "rm -rf .tmp coverage node_modules dist",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "package": "ncc build src/index.ts -o dist --license licenses.txt",
    "prepare": "husky"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.5",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^24.0.3",
    "@vercel/ncc": "^0.38.3",
    "@vitest/coverage-v8": "^3.2.4",
    "husky": "^9.1.7",
    "js-yaml": "^4.1.0",
    "lint-staged": "^17.0.5",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "lint-staged": {
    "*": ["biome check --write --no-errors-on-unmatched"]
  }
}
